source 'http://rubygems.org/'

gem 'sinatra'
gem 'sprockets'
gem 'rake', :require => false

group :development do
  gem 'heroku'
  gem 'shotgun'
end

group :test do
  gem 'rack-test', :require => 'rack/test'
end
